import { makeStyles } from '@material-ui/core/styles';

const styledReceiptActions = makeStyles(() => ({
  root: {
    display: 'flex',
    justifyContent: 'center',
  },
}));

export default styledReceiptActions;
